{
  "name": "deploy",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "pm2": "^5.3.1"
  },
  "scripts": {
    "env": "cp ../.env ../client/.env && cp ../.env ../server/.env",
    "stop": "pm2 stop all",
    "prod": "yarn stop && pm2 start --env production"
  }
}
