{
  "dependencies": {
    "pm2": "^5.3.1"
  },
  "scripts": {
    "env": "cp ../.env ../client/.env && cp ../.env ../server/.env",
    "stop": "pm2 stop all",
    "prod": "pm2 start --env production"
  }
}
